# Bootstrap

## Requirements

List required tools and authoritative versions.

Example:

```text
Runtime: defined by the repository version file
Package manager: available on PATH
Optional tools: exposed through environment variables
```

## Setup

Document the shortest reproducible setup path.

## Diagnostics

Document commands that verify the environment without mutating external systems.

Do not store credentials or machine-specific secrets in this file.
