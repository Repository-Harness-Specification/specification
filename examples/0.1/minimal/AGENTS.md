# Agent Instructions

This repository uses the Repository Harness Specification draft `0.1`.

Before changing code:

1. Read `.harness/harness.yaml`.
2. Select the route that best matches the task.
3. Load only the documents registered by that route.
4. Run the route's required validation commands before reporting completion.

Do not claim a validation succeeded unless the command was executed successfully.
