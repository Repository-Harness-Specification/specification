# Minimal Example

This example demonstrates the smallest useful draft `0.1` Repository Harness.

It contains:

- a root `AGENTS.md` entry point;
- `.harness/harness.yaml`;
- four human-readable capability documents;
- one validation script.

Copy the structure into a sample repository and replace the placeholder commands and project rules.
