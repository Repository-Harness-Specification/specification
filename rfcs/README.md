# RFCs

RFCs are used for significant changes to the Repository Harness Specification.

Use an RFC when proposing changes to:

- manifest structure;
- required capabilities;
- conformance rules;
- precedence;
- extension mechanisms;
- compatibility expectations;
- benchmark methodology.

Copy `0000-template.md` and replace `0000` with the next available number.
